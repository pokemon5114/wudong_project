# 乌东游客向导 —— 环境变量样例
#
# 这个文件只是说明，**不会被自动读取**。请按需设置真实环境变量。
# 千万别把真实 key 提交进仓库。

# DSH 的 DeepSeek Provider 从环境变量读 key。
# 凭据优先级：进程环境变量 > ~/.dsh/.credentials.yaml > 启动器 .env
# 所以只要这里设了，DSH 界面里填的值会被静默忽略 —— 只保留这一处是刻意为之。
# 不设置时，launch.ps1 会回落到 Claude Code 的 ~/.claude/settings.json 复用现有 key。
DEEPSEEK_API_KEY=

# 可选：把只读数据面的地址改指别处（默认 http://127.0.0.1:8001）。
# 这是运维开关；模型无法影响它，它也就是本插件的安全边界。
# WUDONG_API_BASE=http://127.0.0.1:8001
